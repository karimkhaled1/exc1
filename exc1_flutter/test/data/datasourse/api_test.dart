import 'package:flutter_test/flutter_test.dart';

///
///##### NOT WORKING DUE TO THE LOCAL HOST  #####
//
// import 'package:exc1_flutter/data/datasourcce/remote/api_datasource.dart';
// import 'package:exc1_flutter/data/models/product_mode.dart';
// import 'package:flutter_test/flutter_test.dart';

void main() {
  test("api test", () {});
  // ApiRepo api = ApiRepo();
  // ProductModel product = ProductModel(id:DateTime.now().millisecondsSinceEpoch, name:"name", price:20);
  // test("api test add product", () async {
  //   var res = await api.addProduct(product);
  //   expect(res.id, equals(product.id));
  // });
  // test("api test get product", () async {
  //   var res = await api.getProducts();
  //   expect(res.where((element) => element.id == product.id).length, equals(1));
  // });
  // test("api test update", () async {
  //   var res = await api.updateProduct(product..name = "updated");
  //   expect(res.id, equals(product.id));
  //   expect(res.name, equals('updated'));
  // });
  // test("api test delete", () async {
  //   await api.deleteProduct(product);
  //   var res = await api.getProducts();
  //   expect(res.where((element) => element.id == product.id).length, equals(0));
  // });
}
